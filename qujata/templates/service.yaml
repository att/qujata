apiVersion: v1
kind: Service
metadata:
  name: {{ include "qujata.fullname" . }}
  labels:
    {{- include "qujata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qujata.selectorLabels" . | nindent 4 }}
