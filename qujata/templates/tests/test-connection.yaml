apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qujata.fullname" . }}-test-connection"
  labels:
    {{- include "qujata.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qujata.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
